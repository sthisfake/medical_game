/** تنظیمات سراسری بازی — هر تغییری اینجا روی کل بازی اثر می‌گذارد */
export const CONFIG = {
  /** زمان هر سؤال (ثانیه) */
  questionSeconds: 60,
  /** فرصت پاسخ برای هر سؤال */
  maxAttempts: 2,
} as const
